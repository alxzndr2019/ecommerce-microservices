// which service it is

describe("ProductService", () => {
  // which funtion are you testing
  describe("CreateProduct", () => {
    // what scenerios are you testing
    test("validate product inputs", () => {});
    test("validate response", async () => {});
  });
});
